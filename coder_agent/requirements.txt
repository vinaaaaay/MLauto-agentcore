# Core AgentCore runtime — version pinned from mcpserver
bedrock-agentcore==1.12.0
bedrock-agentcore-starter-toolkit==0.3.9

# AWS SDK — version pinned from mcpserver
boto3==1.43.18

# LangGraph + LangChain
langgraph>=0.2.0
langchain-openai>=0.3.0
langchain-openrouter==0.2.3

# MCP SDK — SSE transport + experimental task API (exec_sandbox delivery=poll)
mcp>=1.23,<2

# HTTP client
httpx>=0.27.0

# Observability
psutil>=5.9.0

# Env loading
python-dotenv>=1.0.0
