[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "datasets"
version = "5.0.0"
description = "Dummy datasets package to bypass build size limits"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["datasets"]
